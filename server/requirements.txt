# Exakt gepinnt für reproduzierbare Builds.
# Hinweis: Es wird bewusst die Standardbibliothek (sqlite3) statt SQLAlchemy
# verwendet (siehe CLAUDE.md / db.py) – daher kein SQLAlchemy-Eintrag.
fastapi==0.115.14
uvicorn[standard]==0.32.1
pydantic==2.13.4
paho-mqtt==2.1.0
jinja2==3.1.6
sse-starlette==2.4.1
python-multipart==0.0.32
